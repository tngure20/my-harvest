-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.admins (
  id uuid NOT NULL,
  email text NOT NULL UNIQUE,
  role text DEFAULT 'admin'::text CHECK (role = ANY (ARRAY['admin'::text, 'super_admin'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT admins_pkey PRIMARY KEY (id),
  CONSTRAINT admins_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.ai_requests (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  farm_entity_id uuid,
  session_id uuid,
  request_type text NOT NULL,
  mode text,
  text_query text,
  image_url text,
  related_farm_record_id uuid,
  status text DEFAULT 'pending'::text,
  response_text text,
  ai_model text,
  confidence_score numeric,
  tags ARRAY,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT ai_requests_pkey PRIMARY KEY (id),
  CONSTRAINT ai_requests_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT ai_requests_farm_entity_id_fkey FOREIGN KEY (farm_entity_id) REFERENCES public.farm_entities(id)
);
CREATE TABLE public.alerts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  level text,
  message text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT alerts_pkey PRIMARY KEY (id)
);
CREATE TABLE public.comments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  post_id uuid NOT NULL,
  user_id uuid NOT NULL,
  content text NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  parent_id uuid,
  author_id uuid,
  CONSTRAINT comments_pkey PRIMARY KEY (id),
  CONSTRAINT comments_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT comments_parent_id_fkey FOREIGN KEY (parent_id) REFERENCES public.comments(id),
  CONSTRAINT comments_author_id_fkey FOREIGN KEY (author_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.communities (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text NOT NULL,
  description text,
  image_url text,
  created_by uuid,
  created_at timestamp without time zone DEFAULT now(),
  creator_id uuid,
  CONSTRAINT communities_pkey PRIMARY KEY (id),
  CONSTRAINT communities_creator_id_fkey FOREIGN KEY (creator_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.community_members (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid,
  community_id uuid,
  role text DEFAULT 'member'::text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT community_members_pkey PRIMARY KEY (id),
  CONSTRAINT community_members_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT community_members_community_id_fkey FOREIGN KEY (community_id) REFERENCES public.communities(id)
);
CREATE TABLE public.farm_activities (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  farm_record_id uuid,
  activity_type text NOT NULL CHECK (activity_type = ANY (ARRAY['planting'::text, 'irrigation'::text, 'fertilization'::text, 'pesticide'::text, 'harvesting'::text, 'equipment_maintenance'::text, 'labor'::text, 'soil_testing'::text, 'observation'::text, 'other'::text])),
  title text NOT NULL,
  notes text,
  start_time timestamp with time zone NOT NULL,
  end_time timestamp with time zone,
  is_completed boolean NOT NULL DEFAULT false,
  is_recurring boolean NOT NULL DEFAULT false,
  recurrence_rule text,
  alarm_at timestamp with time zone,
  alarm_sent boolean NOT NULL DEFAULT false,
  assigned_workers ARRAY DEFAULT '{}'::text[],
  equipment_used ARRAY DEFAULT '{}'::text[],
  inputs_used text,
  estimated_cost numeric,
  actual_cost numeric,
  cost_currency text DEFAULT 'KES'::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT farm_activities_pkey PRIMARY KEY (id),
  CONSTRAINT farm_activities_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT farm_activities_farm_record_id_fkey FOREIGN KEY (farm_record_id) REFERENCES public.farm_records(id)
);
CREATE TABLE public.farm_alerts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  farm_id uuid,
  title text NOT NULL,
  message text NOT NULL,
  alert_type text,
  severity text DEFAULT 'medium'::text,
  region text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT farm_alerts_pkey PRIMARY KEY (id),
  CONSTRAINT farm_alerts_farm_id_fkey FOREIGN KEY (farm_id) REFERENCES public.farms(id)
);
CREATE TABLE public.farm_entities (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  farm_id uuid NOT NULL,
  entity_type text NOT NULL,
  name text NOT NULL,
  subtype text,
  quantity numeric DEFAULT 1,
  stage text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT farm_entities_pkey PRIMARY KEY (id),
  CONSTRAINT farm_entities_farm_id_fkey FOREIGN KEY (farm_id) REFERENCES public.farms(id)
);
CREATE TABLE public.farm_notifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  notification_type text NOT NULL CHECK (notification_type = ANY (ARRAY['task_due'::text, 'harvest_reminder'::text, 'pest_alert'::text, 'ai_result'::text, 'weather'::text, 'system'::text])),
  title text NOT NULL,
  message text NOT NULL,
  action_url text,
  related_record_id uuid,
  related_activity_id uuid,
  is_read boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT farm_notifications_pkey PRIMARY KEY (id),
  CONSTRAINT farm_notifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT farm_notifications_related_record_id_fkey FOREIGN KEY (related_record_id) REFERENCES public.farm_records(id),
  CONSTRAINT farm_notifications_related_activity_id_fkey FOREIGN KEY (related_activity_id) REFERENCES public.farm_activities(id)
);
CREATE TABLE public.farm_records (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  name text NOT NULL,
  description text,
  record_type text NOT NULL DEFAULT 'crop'::text CHECK (record_type = ANY (ARRAY['crop'::text, 'livestock'::text, 'poultry'::text, 'aquaculture'::text, 'beekeeping'::text, 'equipment'::text, 'other'::text])),
  crop_type text,
  area_planted numeric,
  area_unit text DEFAULT 'acres'::text,
  growth_stage text CHECK (growth_stage = ANY (ARRAY['germination'::text, 'seedling'::text, 'vegetative'::text, 'flowering'::text, 'fruiting'::text, 'harvest'::text, 'post-harvest'::text, NULL::text])),
  sowing_date date,
  expected_harvest_date date,
  actual_harvest_date date,
  expected_yield numeric,
  actual_yield numeric,
  yield_unit text DEFAULT 'kg'::text,
  irrigation_schedule jsonb DEFAULT '[]'::jsonb,
  fertilizer_schedule jsonb DEFAULT '[]'::jsonb,
  pesticide_schedule jsonb DEFAULT '[]'::jsonb,
  health_status text NOT NULL DEFAULT 'healthy'::text CHECK (health_status = ANY (ARRAY['healthy'::text, 'at_risk'::text, 'affected'::text])),
  pest_disease_flags ARRAY DEFAULT '{}'::text[],
  ai_diagnosis_status text NOT NULL DEFAULT 'none'::text CHECK (ai_diagnosis_status = ANY (ARRAY['none'::text, 'pending'::text, 'processing'::text, 'done'::text, 'failed'::text])),
  media_urls ARRAY DEFAULT '{}'::text[],
  assigned_workers ARRAY DEFAULT '{}'::text[],
  equipment_used ARRAY DEFAULT '{}'::text[],
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'completed'::text, 'archived'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT farm_records_pkey PRIMARY KEY (id),
  CONSTRAINT farm_records_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.farm_tasks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  farm_entity_id uuid,
  user_id uuid,
  title text NOT NULL,
  description text,
  due_date timestamp with time zone,
  priority text DEFAULT 'medium'::text,
  status text DEFAULT 'pending'::text,
  ai_generated boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT farm_tasks_pkey PRIMARY KEY (id),
  CONSTRAINT farm_tasks_farm_entity_id_fkey FOREIGN KEY (farm_entity_id) REFERENCES public.farm_entities(id),
  CONSTRAINT farm_tasks_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.farms (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  name text NOT NULL,
  farm_type text NOT NULL,
  location text,
  size numeric,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT farms_pkey PRIMARY KEY (id),
  CONSTRAINT farms_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.marketplace_listings (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  title character varying NOT NULL,
  description text,
  price numeric,
  location character varying,
  image_url text,
  contact_info text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT marketplace_listings_pkey PRIMARY KEY (id),
  CONSTRAINT marketplace_listings_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.media (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  post_id uuid,
  type text CHECK (type = ANY (ARRAY['image'::text, 'video'::text])),
  url text NOT NULL,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT media_pkey PRIMARY KEY (id)
);
CREATE TABLE public.notifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  type text NOT NULL,
  message text,
  is_read boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  reference_id uuid,
  CONSTRAINT notifications_pkey PRIMARY KEY (id),
  CONSTRAINT notifications_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.post_comments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  post_id uuid NOT NULL,
  user_id uuid NOT NULL,
  text text NOT NULL,
  parent_comment_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT post_comments_pkey PRIMARY KEY (id),
  CONSTRAINT post_comments_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT post_comments_parent_comment_id_fkey FOREIGN KEY (parent_comment_id) REFERENCES public.post_comments(id)
);
CREATE TABLE public.post_reactions (
  post_id uuid NOT NULL,
  user_id uuid NOT NULL,
  type text NOT NULL CHECK (type = ANY (ARRAY['like'::text, 'dislike'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT post_reactions_pkey PRIMARY KEY (post_id, user_id),
  CONSTRAINT post_reactions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.posts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  community_id uuid NOT NULL,
  content text,
  media_url text,
  media_type text,
  image_url text,
  video_url text,
  original_post_id uuid,
  likes_count integer NOT NULL DEFAULT 0,
  dislikes_count integer NOT NULL DEFAULT 0,
  comments_count integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  is_deleted boolean DEFAULT false,
  author_id uuid,
  CONSTRAINT posts_pkey PRIMARY KEY (id),
  CONSTRAINT posts_community_id_fkey FOREIGN KEY (community_id) REFERENCES public.communities(id),
  CONSTRAINT posts_original_post_id_fkey FOREIGN KEY (original_post_id) REFERENCES public.posts(id),
  CONSTRAINT posts_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT posts_author_id_fkey FOREIGN KEY (author_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  email text,
  full_name text,
  role text DEFAULT 'user'::text,
  country text,
  region text,
  farm_scale text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.reactions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid,
  post_id uuid,
  type text CHECK (type = ANY (ARRAY['like'::text, 'dislike'::text])),
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT reactions_pkey PRIMARY KEY (id),
  CONSTRAINT reactions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.shares (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid,
  post_id uuid,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT shares_pkey PRIMARY KEY (id),
  CONSTRAINT shares_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.system_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  log_type text,
  message text NOT NULL,
  meta jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT system_logs_pkey PRIMARY KEY (id),
  CONSTRAINT system_logs_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.tasks (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid,
  title text,
  priority text,
  status text DEFAULT 'pending'::text,
  due_date date,
  ai_generated boolean DEFAULT false,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT tasks_pkey PRIMARY KEY (id)
);
CREATE TABLE public.weather_cache (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  farm_id uuid,
  location text NOT NULL,
  weather_data jsonb NOT NULL,
  fetched_at timestamp with time zone DEFAULT now(),
  CONSTRAINT weather_cache_pkey PRIMARY KEY (id),
  CONSTRAINT weather_cache_farm_id_fkey FOREIGN KEY (farm_id) REFERENCES public.farms(id)
);